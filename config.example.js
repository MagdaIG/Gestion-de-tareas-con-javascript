// Configuración de ejemplo para Supabase
// Copia este archivo como config.js y actualiza con tus credenciales

const SUPABASE_CONFIG = {
  // Tu URL de Supabase (Settings → API → Project URL)
  url: 'https://tu-proyecto.supabase.co',

  // Tu anon key (Settings → API → Project API keys → anon)
  anonKey: 'tu-anon-key-aqui'
};

// Para usar esta configuración, reemplaza en gestionTareas.js:
// const SUPABASE_URL = SUPABASE_CONFIG.url;
// const SUPABASE_ANON_KEY = SUPABASE_CONFIG.anonKey;
